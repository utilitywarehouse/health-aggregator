# --------------------------------------------------------------------------------------------------
# App Variables - required for the build in CI nothing else should be in this file
# --------------------------------------------------------------------------------------------------
APP_NAME := health-aggregator
APP_DESCRIPTION := Calls /__/health for services that expose the endpoint and aggregates the responses
